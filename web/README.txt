Fix build error: apiFetchAuthed() returns fetch Response in this MVP.
Update /src/app/me/account/page.tsx to parse JSON before setState.

Apply:
1) unzip this patch into your /opt/repetitor_app_web (it will overwrite the file)
2) npm run build
